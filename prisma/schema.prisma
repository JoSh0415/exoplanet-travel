datasource db {
  provider          = "postgresql"
  url               = env("DATABASE_URL")
  directUrl         = env("DIRECT_DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Exoplanet {
  id              String   @id @default(cuid())
  name            String   @unique
  distance        Float
  temperature     Float?
  gravity         Float?
  vibe            String?
  discoveryYear   Int?
  bookings        Booking[]
}

model User {
  id            String    @id @default(cuid())
  email         String    @unique
  name          String?
  role          String    @default("USER")
  passwordHash  String
  bookings      Booking[]
}

model DataImportRun {
  id            String   @id @default(cuid())
  sourceName    String
  tapQuery      String
  retrievedAt   DateTime
  insertedCount Int
  updatedCount  Int
  errorMessage  String?
  createdAt     DateTime @default(now())
}

model Booking {
  id            String    @id @default(cuid())
  userId        String
  user          User      @relation(fields: [userId], references: [id])
  planetId      String
  planet        Exoplanet @relation(fields: [planetId], references: [id])
  travelClass   String
  status        String    @default("CONFIRMED")
  bookingDate   DateTime  @default(now())

  @@index([userId])
  @@index([planetId])
}